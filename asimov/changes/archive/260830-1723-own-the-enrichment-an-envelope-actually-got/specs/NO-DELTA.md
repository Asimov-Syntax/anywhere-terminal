No externally verifiable behaviour changes: the window already owes itself a preview pass when it
reopens, and the accepted requirement "a row drawn on every projection is looked at within a bounded
number of them" is what this makes true again after a projection's preview half is skipped. What
moves is which internal fact the host consults, not what a user can observe as a new capability.
