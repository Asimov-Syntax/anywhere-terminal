# NO-DELTA

No external capability changes. A contested session already produces a row; this change only stops
that row losing a name it had already resolved. No new setting, command, message, or contract.
