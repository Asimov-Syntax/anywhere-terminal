# No spec delta

`worktree-panel/spec.md` § "A collision names the result without a second full path" already mandates this behaviour — *"The collision SHALL be stated in one line that names the result, and SHALL NOT restate a full path a second time."* The shipped code does not meet it. No externally mandated constraint changes; the constraint is enforced.
