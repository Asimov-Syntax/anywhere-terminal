# No spec delta

The refusal a person reads still names every member of its contest — that requirement is accepted
and unchanged (`worktree-panel`, "A collision the extension cannot attribute to its own write is
refused"). What changes is where the membership lives on the wire: once per contest instead of once
per member per contest. No externally verifiable behaviour and no externally mandated constraint
moves.
